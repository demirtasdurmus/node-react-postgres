{
  "name": "react-auth",
  "version": "1.0.0",
  "description": "A node react boielerplate",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"cd apiserver && nodemon index.js\" \"cd client && npm start\" ",
    "prod": "concurrently \"cd apiserver && nodemon index.js\" ",
    "build": "concurrently \"cd client && npm run build\" ",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^7.0.0"
  }
}