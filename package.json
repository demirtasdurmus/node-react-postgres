{
  "name": "react-auth",
  "version": "1.0.0",
  "description": "A node react boielerplate",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"cd apiserver && nodemon index.js\" \"cd client && npm start\" ",
    "dev": "concurrently \"cd apiserver && nodemon index.js\" ",
    "prod": "concurrently \"cd apiserver && node index.js\" ",
    "build": "concurrently \"cd client && npm run build\" ",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^7.0.0"
  }
}